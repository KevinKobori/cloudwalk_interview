import 'package:nasa_apod_app/nasa_apod_app.dart';

class KAppEdgeInsets {
  static const allNone = AppEdgeInsets.all(AppGapSizeType.none);
  static const allExtraSmall = AppEdgeInsets.all(AppGapSizeType.extraSmall);
  static const allSmall = AppEdgeInsets.all(AppGapSizeType.small);
  static const allSemiSmall = AppEdgeInsets.all(AppGapSizeType.semiSmall);
  static const allMedium = AppEdgeInsets.all(AppGapSizeType.medium);
  static const allSemiLarge = AppEdgeInsets.all(AppGapSizeType.semiLarge);
  static const allLarge = AppEdgeInsets.all(AppGapSizeType.large);
  static const allExtraLarge = AppEdgeInsets.all(AppGapSizeType.extraLarge);
}
